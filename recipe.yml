name: 'WissKI Sweet'
description: 'WissKI environment with default data model.'
type: 'Site'

recipes:
  - wisski_grain_yeast_water

install:
  - geofield
  - leaflet

config:
  import:
    wisski: "*"
    geofield: "*"
    leaflet: "*"
  actions:
    system.image:
      simple_config_update:
        toolkit: imagemagick

